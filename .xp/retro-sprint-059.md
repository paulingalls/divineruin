# Sprint 59 retrospective

Two milestones shipped. M30 (doc-truth) makes the docs describe the system that shipped. A guard over 61 docs fails when a doc names a removed or never-built tool as live, and it reads fenced code too. README and REMAINING counts are recomputed from the phase docs. Doc facts that code owns are read from that code. INDEX.md is generated by `scripts/doc_index.py` with a drift test. M39 (the last player out ends the session) lets a guest's goodbye end only their seat. A host who leaves hands the table to the longest-present player, and a player who left can rejoin with a new invite. M38 was also closed: its Done-when is now runnable. Six cards landed against a cap of six.

**What the process caught.** The story reviews caught defects that the executors' own tests passed over. `ExplorationAgent.on_agent_turn_completed` was never a LiveKit hook, so before this sprint a spoken goodbye never closed the session. Story-116 now has a guard over the real `Agent` base. Other catches:
- The doc guard could not see fenced code.
- The INDEX generator silently dropped a new directory and was a syntax error on Python 3.11.
- A departure raced with a disconnect and paused the background process for the remaining players.
- An interrupted farewell left a ghost host, so the session could never close. The lead reclassified that "noted" finding as blocking and sent it back.

The plan reviewer stopped story-116 on an AC that the installed LiveKit makes impossible to meet: gameplay RoomIO has no audio input.

**What it missed.** Two of my card claims were wrong before any code ran (constraint 10 again): the INDEX baseline count, and "P2's audio is the linked input". The slate review caught the first and the plan review the second. The commit hook only ran `ast.parse` over `scripts/*.py` with the system Python 3.14, so 3.12-only syntax passed the commit. Only the story reviewer caught it.

**Rules.** Constraint 9 (call the vendor's real thing) earned its place three times: the hook name, RoomIO's audio input, and revoke_token_ts, which self-hosted LiveKit does not enforce. Constraint 10 still costs rounds because I write baselines from memory. The refresh leg and the plan review are what make it cheap.

**Executable diff.** `scripts/test-fast.sh` now runs `ruff check` and `ruff format --check` with apps/agent's py311 config over every staged non-agent Python file, replacing the system-python `ast.parse`. `scripts/test-fast-tier.sh` pins both calls and reds against the old hook. Before this change, `f"{"\n".join(x)}"` in `scripts/` committed cleanly. Now the commit fails.
