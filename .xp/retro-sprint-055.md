# Sprint 55 retrospective

The six completed cards make combat sound events audible and constrain training narration to actual returned state and eligibility. Both newly generated cues use the human-selected v1 takes. Four added bug cards addressed demonstrated lifecycle/environment faults and important eligibility coverage; no debt was carried under a false label.

The most serious failure was operational: story090 teardown accepted copied primary lifecycle settings and deleted the original local primary volume. The original data was not recovered. The retained archive contains only the database recreated after that incident. Restoring that archive and reseeding established a usable development environment; it did not undo the loss.

The executable correction is in092/093: one authority validates checkout/clone identity, resource labels, running service publication, listener ownership and runtime endpoints before connecting or mutating. Adapters fail loudly; negative fixtures exercise copied settings, foreign listeners, unreadable metadata and incomplete enumeration. Hook lanes preserve their intended endpoints and keep external credentials out of unit tests. These changes replace trust in copied environment configuration; no additional generic caution rule is needed.

Review earned its cost where it exercised the actual runtime. Bun registry tests alone missed Metro/Watchman exclusion of shared sound content. The real Expo export exposed it. Mutation checks exposed trace assertions that could not fail against their stated defects; reachable positive neighbors now prevent an empty or over-restrictive guard from certifying itself. A duplicated sound export list was removed rather than maintained alongside shared content.

Training failures were not dismissed as variance. Captured false judge assertions were narrowed with real positive and negative calibration, preserving tool-result, order and remaining-time requirements. A truncated earlier martial-judge failure has no established cause; diagnostic passes do not explain it. Final corrected story validation completed five consecutive full training-file passes with zero skips, then repeated that evidence in confirming review. Integration against updated dependencies was validated separately before landing.

The incoming main dependency upgrade overlaps bootstrap and test-environment behavior. Its frozen installs, version checks and explicit missing-DSN failures are retained alongside the stronger ownership rules. The ownership test fixture now supplies actual version manifests and delegates the version probe to installed Bun; its installation stop remains effective. The combined tree passed lint, the full unit aggregate, real ownership/listener tests, the actual Expo export and the full pre-push gate. Final sprint review covers these resolutions before release.

The sprint review also exposed two false-success paths: environment overrides could select the internal pre-push harness, and failed Python teardown cleared ownership. The hook now requires an explicit internal invocation and refuses production overrides; cleanup checks require a nonempty, valid child-PID record. Real Docker fixtures exercise a foreign Valkey listener through both adapters. Story094 preserves zero active callers and retryable ownership when teardown fails. A real Docker counterexample then showed why a retained boolean alone was insufficient: a later session could delete a manually restarted stack. The correction binds ownership to observed service lifetimes and compares them again before cleanup, including the missing-state fallback. Real restart/recreation cases and an unchanged-stack retry passed on the landed tree; all six cards are merged and pushed. This replaces stale ownership claims with evidence for the actual services being removed.

The configured worktree bootstrap was missing even though the script existed. `.xp/system.md` now names `bash scripts/init-worktree.sh`; the actual plugin parser and an isolated bootstrap verified the correction. This replaces manual preparation of newly spawned worktrees.

Lifecycle limits remain explicit: failed startup or incomplete teardown can leave owned resources running when their full lifetime can no longer be verified. The harness raises and preserves evidence; it does not infer permission to delete uncertain resources.

Deployment handoff: before deploying the agent, audit existing live spell-training rows for missing, unknown or ineligible spell IDs (note e3f399d4). No production audit or deletion was performed in this sprint. This is not a release-blocking claim that a production defect was observed.

Resolved behavioral records remain active: their recurring guarantees are retained. Completed planning and review notes are archived; future feature expansions remain unshipped. The constraints already require real-contract tests, fault injection and fail-loud behavior, so no duplicate rule is added. The executable change is the shared resource authority and its adapter/teardown tests, displacing independent lifecycle trust in copied settings.

The four original milestone debts had completed cards but missing resolution records. Their exact card checks passed on the landed tree and now back the recorded resolutions. Milestone37 stays open until its remaining historical record dispositions are reconciled; card completion alone does not establish its broader completion condition.
