{
  "name": "divineruin-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../../.env.local --hot src/index.ts",
    "start": "NODE_ENV=production bun src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@livekit/protocol": "^1.44.1",
    "livekit-server-sdk": "^2.15.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
