# dependencies (bun install)
node_modules

# output
out
dist
*.tgz

# code coverage
coverage
*.lcov

# logs
logs
*.log
apps/agent/transcripts/
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local
# Any .env sidecar (e.g. the .env.bak-<stamp> the env top-up writes) — .env.example
# is the one tracked member of this family.
.env.*
!.env.example

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# tmp
tmp/

# Python
__pycache__
*.pyc
.pytest_cache
.ruff_cache
.venv
.coverage
coverage.json

# Playwright
e2e/test-results/
e2e/playwright-report/
e2e/node_modules/

# Pre-push gate flake logs — every lane tees here; failures snapshot to a
# timestamped subdir. Repo-root home (unified from the former e2e/flake-artifacts/).
flake-artifacts/

# Test results (root)
test-results/

# Generated assets
assets/audio/activity_*.mp3
assets/images/img_*.png

# Vim
*.un~

# Claude Code
.claude/
.agents/
skills-lock.json

# Watchman cookie files (transient probes dropped in the repo root by watchman)
.watchman-cookie-*
